# Benthos Presentation Support Code

## Requirements

 * docker
 * docker-compose

## Running

`docker-compose up -d --remove-orphans`

## Kibana

Once running, visit [localhost:5601](http://localhost:5601).

## Stopping

``docker-compose down --remove-orphans`
